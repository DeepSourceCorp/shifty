name: Build

on:
  pull_request:
    branches: [master]

jobs:
  build:
    runs-on: blacksmith-4vcpu-ubuntu-2404

    steps:
      - uses: actions/checkout@v2

      - name: Install packages
        run: yarn

      - name: Build shifty
        run: yarn build
